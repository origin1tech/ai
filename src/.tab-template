<div class="tabpanel">

      <ul class="nav nav-tabs">
            <li ng-class="{ active: tabActive('decimal:markup') }"><a ng-click="showTab('decimal:markup')">Markup</a></li>
            <li ng-class="{ active: tabActive('decimal:code') }"><a ng-click="showTab('decimal:code')">Code</a></li>
            <li ng-class="{ active: tabActive('decimal:options') }"><a ng-click="showTab('decimal:options')">Options</a></li>
      </ul>

      <div class="tab-content">

          <div ng-class="{ active: tabActive('decimal:markup') }" ng-show="tabActive('decimal:markup')">

          </div>

          <div ng-class="{ active: tabActive('decimal:code') }" ng-show="tabActive('decimal:code')">

          </div>

          <div ng-class="{ active: tabActive('decimal:options') }" ng-show="tabActive('decimal:options')">

          </div>

      </div>

</div>


<div class="tabpanel">

    <ul class="nav nav-tabs">
        <li ng-class="{ active: tabs.markup.active }"><a ng-click="showTab('markup')">Markup</a></li>
        <li ng-class="{ active: tabs.script.active }"><a ng-click="showTab('script')">Script</a></li>
        <li ng-class="{ active: tabs.options.active }"><a ng-click="showTab('options')">Options</a></li>
    </ul>

    <div class="tab-content">

        <div ng-class="{ active: tabs.markup.active }" ng-show="tabs.markup.active">
            <h4>Markup</h4>

        </div>

        <div ng-class="{ active: tabs.script.active }" ng-show="tabs.script.active">
            <h4>Code</h4>

        </div>

        <div ng-class="{ active: tabs.options.active }" ng-show="tabs.options.active">
            <h4>Options</h4>

        </div>

    </div>

</div>